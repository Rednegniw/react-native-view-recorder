---
title: Getting Started
description: Let's install and set up React Native View Recorder in your project.
full: true
---

## Installation

<Steps>
<Step>
### Install the library

<Tabs items={['expo', 'bun', 'npm', 'yarn', 'pnpm']}>
<Tab value="expo">
```bash
npx expo install react-native-view-recorder
```
</Tab>
<Tab value="bun">
```bash
bun add react-native-view-recorder
```
</Tab>
<Tab value="npm">
```bash
npm install react-native-view-recorder
```
</Tab>
<Tab value="yarn">
```bash
yarn add react-native-view-recorder
```
</Tab>
<Tab value="pnpm">
```bash
pnpm add react-native-view-recorder
```
</Tab>
</Tabs>
</Step>
<Step>
### Configure the Expo plugin (Expo projects)

Add the plugin to your app config:

```json title="app.json"
{
  "expo": {
    "plugins": ["react-native-view-recorder"]
  }
}
```
</Step>
<Step>
### Rebuild your dev client

```bash
npx expo run:ios
npx expo run:android
```

<Callout type="warn">
Expo Go does not include custom native modules. You need a development build (Expo Dev Client or bare React Native).
</Callout>
</Step>
</Steps>

## Peer dependencies

| Package      | Version   | Required |
| ------------ | --------- | -------- |
| react        | >= 18     | Yes      |
| react-native | >= 0.73   | Yes      |
| expo         | >= 54.0.0 | Optional |
